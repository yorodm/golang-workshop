# Operadores

Golang soporta la mayoria de los operadores que tienen otros lenguajes. Se dividen en dos categorias:

## Numericos

`+` => Addition

`-` => Subtraction

`/` => Division

`*` => Multiplication

`%` => Reminder

## Boleanos

`!`  => Not

`&&` => And

`||` => Or